# The Condo Book Project (repo: openbook)

Static site deployed on Vercel from main. No build step for the main pages.

- Main page: index.html (markup, styles and script inline). Shared styles: bureau.css, pages.css, menu.css.
- Search behavior may be improved, but keep the Supabase integration working: same project URL and publishable key, existing RPCs (search_plans_v2, map_points, plan_detail, plan_thumbs, plan_sections). Don't add write calls or new keys. Database changes are out of scope.
- buildings/*.html are generated by scripts/build-buildings.mjs from scripts/masthead.html. Don't hand-edit buildings/*.html; change the generator.
- Keep element ids the scripts rely on, ?q= search-on-load, and light and dark themes.
- Never invent prices, sales, professionals or document citations.
- Don't commit, push or deploy unless asked.
